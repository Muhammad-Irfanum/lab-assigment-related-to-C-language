/*
                     // formula i
#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	int r = (a-b)*(a+b);
	printf("(%d-%d)(%d+%d)= %d",a,b,a,b,r);
	
	return 0;
}
*/

/*
                     // formula ii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = (a*a) + 2*(a*b) + (b*b);
	printf(" %d*%d + 2*%d*%d + %d*%d = %d",a,a,a,b,b,b,r);
	
	return 0;
} 
*/

/*
                     // formula iii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = (a*a + b*b -2*a*b) + 2*a*b;
	printf("(%d*%d + %d*%d - 2*%d*%d) + 2*%d*%d = %d",a,a,b,b,a,b,a,b,r);
	
	return 0;
}
*/

/*
                     // formula iv
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a -2*a*b +b*b;
	printf(" %d*%d - 2*%d*%d + %d*%d = %d",a,a,a,b,b,b,r);
	
	return 0;
}
*/

/*
                     // formula v
#include<stdio.h>
int main()
{
	int a,b,c,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	printf("Enter c value :");
	scanf("%d",&c);
	
	r = a*a +b*b +c*c +2*a*b +2*b*c +2*c*a;
	printf(" %d*%d + *%d*%d + %d*%d + 2*%d*%d + 2*%d*%d +2*%d*%d = %d",a,a,b,b,c,c,a,b,b,c,c,a,r);
	
	return 0;
}
*/

/*

                     // formula vi
#include<stdio.h>
int main()
{
	int a,b,c,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	printf("Enter c value :");
	scanf("%d",&c);
	
	r = a*a +b*b +c*c -2*a*b +2*b*c -2*c*a;
	printf(" %d*%d + *%d*%d + %d*%d - 2*%d*%d + 2*%d*%d -2*%d*%d = %d",a,a,b,b,c,c,a,b,b,c,c,a,r);
	
	return 0;
}
*/

/*
                   // formula vii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a +3*a*a*b +3*a*b*b +b*b*b;
	printf(" %d*%d*%d + 3*%d*%d*%d +3*%d*%d*%d + *%d*%d*%d = %d",a,a,a,a,a,b,a,b,b,b,b,b,r);
	
	return 0;
} 
*/

/*

                   // formula viii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a -3*a*a*b +3*a*b*b -b*b*b;
	printf(" %d*%d*%d - 3*%d*%d*%d +3*%d*%d*%d - *%d*%d*%d = %d",a,a,a,a,a,b,a,b,b,b,b,b,r);
	
	return 0;
} 
*/

/*
                 // formula ix
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = (a-b)*(a*a +a*b +b*b);
	printf("(%d-%d)(%d*%d + %d*%d + *%d*%d) = %d",a,b,a,a,a,b,b,b,r);
	
	return 0;
}
*/


/*
                 // formula x
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = (a+b)*(a*a -a*b +b*b);
	printf("(%d+%d)(%d*%d - %d*%d + *%d*%d) = %d",a,b,a,a,a,b,b,b,r);
	
	return 0;
}
*/

/*
                 // formula xi
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a + 3*a*a*b +3*a*b*b + b*b*b;
	printf("%d*%d*%d + 3*%d*%d*%d + 3*%d*%d*%d + %d*%d*%d = %d",a,a,a,a,a,b,a,b,b,b,b,b,r);
	
	return 0;
}
*/

/*
                 // formula xii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a - 3*a*a*b +3*a*b*b - b*b*b;
	printf("%d*%d*%d - 3*%d*%d*%d + 3*%d*%d*%d - %d*%d*%d = %d",a,a,a,a,a,b,a,b,b,b,b,b,r);
	
	return 0;
}
*/

/*
                 // formula xiii
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a*a + 4*a*a*a*b +6*a*a*b*b +4*a*b*b*b + b*b*b*b;
	printf("%d*%d*%d*%d + 4*%d*%d*%d*%d + 6*%d*%d*%d*%d + 4*%d*%d*%d*%d + %d*%d*%d*%d = %d",a,a,a,a,a,a,a,b,a,a,b,b,a,b,b,b,b,b,b,b,r);
	
	return 0;
}
*/

/*
             // formula xiv
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = a*a*a*a - 4*a*a*a*b +6*a*a*b*b -4*a*b*b*b + b*b*b*b;
	printf("%d*%d*%d*%d - 4*%d*%d*%d*%d + 6*%d*%d*%d*%d - 4*%d*%d*%d*%d + %d*%d*%d*%d = %d",a,a,a,a,a,a,a,b,a,a,b,b,a,b,b,b,b,b,b,b,r);
	
	return 0;
}
*/

/*
                 // formula xv
#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	int r = (a-b)*(a+b)*(a*a + b*b);
	printf("(%d-%d)(%d+%d)(%d*%d + %d*%d)= %d",a,b,a,b,a,a,b,b,r);
	
	return 0;
}
*/


         // formula xvi
#include<stdio.h>
int main()
{
	int a,b,r;
	printf("Enter A value :");
	scanf("%d",&a);
	printf("Enter B value :");
	scanf("%d",&b);
	
	r = (a-b)*(a*a*a*a + a*a*a*b + a*a*b*b + a*b*b*b + b*b*b*b);
	printf("(%d - %d)(%d*%d*%d*%d + %d*%d*%d*%d + %d*%d*%d*%d + *%d*%d*%d*%d + %d*%d*%d*%d) = %d",a,b,a,a,a,a,a,a,a,b,a,a,b,b,a,b,b,b,b,b,b,b,r);
	
	return 0;
}
